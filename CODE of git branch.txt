
mkdir git-project
cd git-project
git init

git switch -c production
git commit --allow-empty -m "Production Commit 1"

git branch hotfix
git branch integration


git switch integration
git switch -c feature1

git commit --allow-empty -m "Feature1 Commit 1"
git commit --allow-empty -m "Feature1 Commit 2"


git switch integration
git switch -c feature2

git commit --allow-empty -m "Feature2 Commit 1"
git commit --allow-empty -m "Feature2 Commit 2"
git commit --allow-empty -m "Feature2 Commit 3"


git switch integration
git merge feature2 --no-ff -m "Merge Feature2"

git switch hotfix
git merge integration --no-ff -m "Merge Integration"

git switch production
git commit --allow-empty -m "Production Commit 2"
git merge hotfix --no-ff -m "Release REL01.00.00"

git tag REL01.00.00


git switch feature1
git rebase integration

git commit --allow-empty -m "Feature1 Commit 4"
git commit --allow-empty -m "Feature1 Commit 5"

git switch integration
git merge feature1 --no-ff -m "Merge Feature1"

git switch hotfix
git merge integration --no-ff -m "Release R02.00.00"

git switch production
git merge hotfix --no-ff -m "Release R02.00.00"

git tag R02.00.00


git switch integration
git commit --allow-empty -m "Integration Commit 4"

git switch hotfix
git merge integration --no-ff -m "Release R02.00.01"

git switch production
git merge hotfix --no-ff -m "Release R02.00.01"

git tag R02.00.01